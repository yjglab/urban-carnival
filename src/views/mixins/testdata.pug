mixin data(data)
    div 
        h3 
            a(href=`/videos/${data.id}`)=data.title 
        ul 
            li #{data.views}
            li #{data.id}
            li Uploaded #{data.createdAt}
            li #{data.comments} comments
